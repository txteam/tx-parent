<?xml version="1.0" encoding="UTF-8"?>
<tableDefinition>
	<tableName>CORE_DBSCRIPT_CONTEXT</tableName>
	<tableVersion>1.0.0</tableVersion>
	
	<dataSourceScript dataSourceType="H2">
		<createTableScript>
			CREATE TABLE CORE_DBSCRIPT_CONTEXT(
				ID VARCHAR(64) NOT NULL,
				TABLENAME VARCHAR(64) NOT NULL,
				TABLEVERSION VARCHAR(64) NOT NULL,
				CREATEDATE TIMESTAMP,
				LASTUPDATEDATE TIMESTAMP,
				PRIMARY KEY(ID)
			);
			CREATE UNIQUE INDEX IDX_CORE_DBS_CON_00 ON CORE_DBSCRIPT_CONTEXT(TABLENAME);
		</createTableScript>
		
		<defaultUpdateTableScript>
			CREATE TABLE CORE_DBSCRIPT_CONTEXT_BAK
				 AS SELECT * FROM CORE_DBSCRIPT_CONTEXT;
			DROP TABLE CORE_DBSCRIPT_CONTEXT;
			CREATE TABLE CORE_DBSCRIPT_CONTEXT(
				ID VARCHAR(64) NOT NULL,
				TABLENAME VARCHAR(64) NOT NULL,
				TABLEVERSION VARCHAR(64) NOT NULL,
				CREATEDATE TIMESTAMP,
				LASTUPDATEDATE TIMESTAMP,
				PRIMARY KEY(ID)
			);
			CREATE UNIQUE INDEX IDX_CORE_DBS_CON_00 ON CORE_DBSCRIPT_CONTEXT(TABLENAME);
		</defaultUpdateTableScript>
	</dataSourceScript>
	
	<dataSourceScript dataSourceType="ORACLE">
		<createTableScript>
			CREATE TABLE CORE_DBSCRIPT_CONTEXT(
				ID VARCHAR2(64) NOT NULL,
				TABLENAME VARCHAR2(64) NOT NULL,
				TABLEVERSION VARCHAR2(64) NOT NULL,
				CREATEDATE DATE,
				LASTUPDATEDATE DATE,
				PRIMARY KEY(ID)
			);
			CREATE UNIQUE INDEX IDX_CORE_DBS_CON_00 ON CORE_DBSCRIPT_CONTEXT(TABLENAME);
		</createTableScript>
		
		<defaultUpdateTableScript>
			CREATE TABLE CORE_DBSCRIPT_CONTEXT_BAK
				 AS SELECT * FROM CORE_DBSCRIPT_CONTEXT;
			DROP TABLE CORE_DBSCRIPT_CONTEXT;
			CREATE TABLE CORE_DBSCRIPT_CONTEXT(
				ID VARCHAR2(64) NOT NULL,
				TABLENAME VARCHAR2(64) NOT NULL,
				TABLEVERSION VARCHAR2(64) NOT NULL,
				CREATEDATE DATE,
				LASTUPDATEDATE DATE,
				PRIMARY KEY(ID)
			);
			CREATE UNIQUE INDEX IDX_CORE_DBS_CON_00 ON CORE_DBSCRIPT_CONTEXT(TABLENAME);
		</defaultUpdateTableScript>
	</dataSourceScript>
</tableDefinition>